// Task 1: Calculate and display the sum of numbers from 1 to 10
// Pseudo Code
// 1. Initialize a variable sum to store the sum of numbers.
// 2. Use a for loop to iterate from 1 to 10.
// 3. Inside the loop, add the current value of i to the sum variable.
// 4. After the loop, display the sum to the console.

/*let sum = 0;

for (let i = 1; i <= 10; i++) {
  sum += i;
}

console.log("The sum of numbers from 1 to 10 is:", sum)*/

//////////////////////////////////////////////////////////////////////////////////////

// Task 2: Generate and display the number pattern
// Pseudo Code 
// 1. Initialize a variable pattern as an empty string to store the pattern.
// 2. Use another for loop to iterate from 1 to 5 (the number of rows in the pattern).
// 3. Inside the loop, concatenate the current value of i to the pattern variable.
// 4. Display the pattern variable on each iteration to create the pattern.

let pattern = '';

for (let i = 1; i <= 5; i++) {
  pattern += i;
  console.log(pattern);
}

// Remove the /* between each line of code when trying to use the program